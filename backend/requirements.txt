fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
bcrypt>=4.1
pyjwt>=2.8
stripe>=10.0
anthropic>=0.34
email-validator>=2.1

# Postgres driver for production (Neon/Render). Local dev uses the
# built-in sqlite3 module, so this is unused until DATABASE_URL points
# at a postgres:// URL.
psycopg2-binary>=2.9

pytest>=8.0
httpx>=0.27
